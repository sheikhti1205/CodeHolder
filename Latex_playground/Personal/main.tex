\documentclass{article}

\usepackage{tikz}
\begin{document}

\begin{figure}[h]
  \begin{center}

    \begin{tikzpicture}
      % \draw[thick] (-2, -2) grid (2, 2);
      % \draw (0, 0) -- (45:{sqrt(2)})
      % -- (2, 0) -- cycle;
      % \draw (0, 0) circle (2cm);
      % \draw [fill, color=black](0, 0) circle (0.5cm);
      % \draw [<->] (-2, 0) node[left] {$A$} -- (2, 0) node[right] {$B$} -- 
      % (0, -2) node[below] {$C$} -- (-2, 0) -- 
      % (0, 2) node[above] {$D$} -- (2, 0) -- cycle;
      % \draw (0, 0) circle (2cm);
      % \draw [line width=2mm] (-2, 0) -- (2, 0);
      % \draw [fill,color=red](0, 0) circle (0.3cm);
      % \coordinate (0) at (-2, -2);
      % \coordinate (0') at (2, -2);
      % \coordinate (A) at (0, 3);
      % \coordinate (B) at (0, 2);
      % \draw (0) -- (A) (B) -- (0);
      % \draw (0') -- (A) (B) -- (0');

      % \draw (2, 0) node[right] {$A$}
      % -- (0, 0) node[below] {$O$}
      % -- (2, 2) node[right] {$B$};
      % \draw[green] (2,2) arc (0:270:2);
      % \draw[red] (2, 0) arc (0:45:2);
      % \draw[blue] (1, 0) arc (0:45:1);

      % \draw (0, 0) arc (270:180:1) arc (90:0:1);
      % \draw (1, 0) arc (-90:0:1) arc (90:180:1);
      % \draw (.75, -0.5) arc (360:0:0.2);
      % \draw (0.5, -0.5) circle [x radius=0.25, y radius=0.1];

      % \draw (-2, -2) -- (-2, 0) -- (0, 0) arc (180:360:1) -- (4, 0);
      % \draw (-2, -2) -- (0, -2) arc (-180:-360:1) -- (4, -2) -- (4, 0);
      % \draw (-2, 0) -- (0, 0) circle (1) -- (2, 0);
      % \draw (-4, -4) -- (0, 0) -- (4, -4)
      % node[near start, fill=white] {$A$}
      % node[midway, fill=white] {$B$}
      % node[near end, fill=white] {$C$};

      % \draw[very thin, gray]
      % (-12, -8) grid (12, 8);

% loosely dotted, 
% densely dotted, dotted, dashed, 
% densely dashed, loosely dashed, double
      % \draw[double, green!30!blue!30!red] (-4, 0) node[below] {$(-4, 0)$} 
      % -- (-2, 0) node[above] {$(-2, 0)$}
      % arc (180:360:1) node[midway, below] {$(-1, -1)$} 
      % node[fill=white] {$(-0, 0)$} -- (0, 0)
      % -- (0, 0) %node[below] {$(4, 0)$} 
      % arc (-180:-360:1) node[midway, above] {$(1, 1)$} node[below] {$(2, 0)$}
      % -- (4, 0) node[below] {$(4, 0)$};

      % \coordinate (A) at (-2, -2);
      % \coordinate (B) at (0, 0);
      % \coordinate (C) at (2, -2);
      % \draw (A) -- ++(4, 4) 
      %  -- (B) -- ++(4, 4)
      %  -- (C) -- ++(4, 4)
      %  -- cycle;
      % \draw (0,0) arc(0:90:1) arc (180:270:1);
      % \draw (0.5,0) arc (180:90:1) arc ( 360:270:1);
      % \draw (0.25,-0.3) 
      % circle[x radius=0.25, y radius=0.1];

      % \coordinate (A) at (0, 0);
      % \coordinate (B) at (0.6, -0.35);
      % \draw (0, 0) node[midway, fill=gray] {$O$};
      % \draw (A) 
      % --++(1, 1) --++(1, -1) -- cycle;
      % \draw (B) 
      % --++(1, 1) --++(1, -1) -- cycle;

      % \clip (0, 0) circle (5);
      % \clip (-0, -6) -- (-6, 0)
      % -- (0, 6) -- (6, 0) -- cycle;

      % \draw[fill=gray!20] (0, 0) circle (5);

      % \draw[white] (-5, -5) grid[step=0.25] (5, 5);

      % \draw[draw=red, fill=green] (-0.5, 2) --++(1, 0) --++(72:1)
      % --++(144:1) --++(216:1) -- cycle;

      % \draw[color=blue, fill=red] (-4, -1) --++(1, 0) --++(60:1)
      % --++(120:1) --++(180:1) --++(240:1)
      % -- cycle;

      % \draw[green, fill=blue] (3, -1) --++(.75, 0) ++(45:.75)
      % --++(90:.75) --++(135:.75) --++(180:.75)
      % --++(225:.75) --++(270:.75) --++(315:.75)
      % --++(360:.75) -- cycle;

      % \clip (-1, 0) circle (2);
      % \clip (1, 0) circle (2);
      
      % \draw[fill=blue!20!green!30!red!30] 
      % (-4, -4) rectangle (4, 4);

      % \clip (-4, -4) rectangle (4, 4);

      % \draw[fill=gray!20] (-6, -6) rectangle (6, 6);
      % \draw[green] (0, 0) circle (1);
      % \draw[red] (1, 0) circle (1);

      % \draw (0, 0) 
      % foreach \a in {0, 2, ..., 20}
      % { -- ({mod(\a,4)/4}, \a/4) 
      % node[fill=white] {$\a$}}
      % ;
      % \draw (0, 5)
      % foreach \b in {20, 22, ..., 40}
      % { -- ({\b/4 - 5}, {(mod(\b,4)/+4) + 5})
      % node[fill=white] {$\b$}}
      % ;
      % \draw (5, 0)
      % foreach \c [evaluate=\c as 
      % \d using int(\c+40)]
      % in {0, 2, ..., 20}
      % { -- ({(-mod(\c,4)/4) +5}, \c/4)
      % node[fill=white]{$\d$}}
      % ;
      
      
      % \draw (-2, -2) -- (0, 0);

      % \foreach \x in {0,2,...,10} \draw
      % ({mod(\x,4)/4},\x/4) node[right] {\x};

      % \foreach \x in {1,...,10}{
      % \foreach \y in {1,...,10}{
      % \ifnum \x>\y
      % \fill[red] (\x,\y) circle (0.2);
      % \else
      % \fill[gray] (\x,\y) circle (0.2);
      % \fi
      % }   
      % }

      
      
    \end{tikzpicture}
  \end{center}
  \caption{Absurd Thing}
\end{figure}

\end{document}